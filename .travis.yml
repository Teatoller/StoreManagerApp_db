language: python

python:
 - "3.6"

install:
 - pip install -r requirements.txt

 - pip install coveralls
env:
 - FLASK_VERSION=1.0.2
before_script:
  - psql -c "create database test_db;" -U postgres
  - psql -c "create user user_1;" -U postgres
script:
 - pytest --cov-report term-missing --cov=app
 - coveralls
services:
  - postgresql
after_success:
 - coveralls